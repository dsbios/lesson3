dist: focal

jobs:
  include:
    - stage: build docker file
      script:
       - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
       - docker build -t $DOCKER_USERNAME/travis-test .
       - docker push $DOCKER_USERMANE/trevis-test
