dist: xentia

jobs:
  include:
    - stage: build docker file
      script:
       - echo "$PASSWORD" | docker login -u "$USERNAME" --password-stdin
       - docker build -t $USERNAME/lesson2 .
       - docker push $USERMANE/lesson2
